name: Daily ROCm build

on:
  workflow_dispatch:
  #schedule:
  #  - cron: "0 14 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        build:
          - rocm_version: "7.14"
            rocm_build: "7.14.0-gfx906+20260802001858"
            release_tag: "20260802001858"
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
        with:
          ref: ${{ matrix.build.release_tag }}

      - name: Aggressive cleanup
        uses: ./.github/workflows/actions/cleanup-space-action

      - name: Install yq
        uses: ./.github/workflows/actions/install-yq-action

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Run build script
        run: |
          export ROCM_IS_RELEASE=$(git describe --exact-match --tags HEAD >/dev/null 2>&1 && echo 1 || echo 0)
          export ROCM_VERSION="${{ matrix.build.rocm_version }}"
          export ROCM_BUILD="${{ matrix.build.rocm_build }}"
          cd ./rocm
          ./build-and-push.image.sh
